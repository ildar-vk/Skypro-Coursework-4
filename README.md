# Запуск проекта

## Требования
- Docker и Docker Compose

## Инструкция

1. Клонировать репозиторий
2. Создать файл `.env` в корне проекта (пример см. в `.env.example`)
3. Выполнить сборку и запуск:
   ```bash
   docker-compose up -d --build